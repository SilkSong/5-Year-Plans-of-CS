# Year 1: 2024

## February

2024.1.31

* 掌握 `pandas.dropna` 函数用法，主要有三个参数：how, inplace, axis
* 掌握当 Series 只有一个元素的时候，如何快速把该值取出：`s.item()`
* 对使用 ssh 连接服务器有了更深的体会，第一次使用 `scp` 进行本地与服务器的文件上传与下载
* 体验使用 nmon 进行计算机性能监控
* 代码
    ```
    with open(filename, 'r') as file"
        nums = [int(line.strip()) for line in file]
    ```

